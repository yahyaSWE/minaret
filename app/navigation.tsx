'use client';
import Link from 'next/link';
import { usePathname } from 'next/navigation';
import { useState } from 'react';
import { ArrowUpRight, Menu, X } from 'lucide-react';
const links=[['Utbildning','/utbildning'],['Böcker','/bocker'],['Kurser','/kurser'],['Kunskapsbank','/kunskapsbank'],['Om Minaret','/om-minaret']];
export function Header(){const [open,setOpen]=useState(false);const path=usePathname();return <header className="header"><div className="header-inner"><Link href="/" className="brand" aria-label="Minaret Folkbildning, startsida" onClick={()=>setOpen(false)}><img src="/minaret-logo.jpeg" alt="Minaret Folkbildning"/></Link><button className="menu-toggle" aria-label={open?'Stäng meny':'Öppna meny'} aria-expanded={open} aria-controls="navigation" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button><nav id="navigation" className={open?'navigation open':'navigation'} aria-label="Huvudmeny">{links.map(([label,url])=><Link href={url} key={url} className={path.startsWith(url)?'active':''} aria-current={path===url?'page':undefined} onClick={()=>setOpen(false)}>{label}</Link>)}<Link href="/kontakt" className="nav-contact" onClick={()=>setOpen(false)}>Kontakta oss <ArrowUpRight size={16}/></Link></nav></div></header>}
export function Footer(){return <footer className="footer"><div className="container footer-top"><div><Link href="/" className="footer-brand">MINARET<span>FOLKBILDNING</span></Link><p>Kunskap. Gemenskap. Utveckling.</p><p className="footer-description">Kunskap och dialog om islam<br/>i en svensk kontext.</p></div><div><span className="footer-label">UTFORSKA</span>{links.slice(0,4).map(([label,url])=><Link key={url} href={url}>{label}</Link>)}</div><div><span className="footer-label">MINARET</span><Link href="/om-minaret">Om oss</Link><Link href="/kontakt">Kontakt</Link><span className="footer-note">En plats för lärande.<br/>En grund för förståelse.</span></div></div><div className="container footer-bottom"><span>© {new Date().getFullYear()} Minaret Folkbildning</span><span>Kunskap som för oss närmare.</span></div></footer>}
